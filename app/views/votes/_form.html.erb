<%= minimal_form_for(@vote) do |f| %>

    <%= f.input :question %>
    <%= f.text_area :details, rows: 3, placeholder: "Add question details (optional)" %>
    <%= f.text_area :choices, rows: 5, placeholder: "Enter answers on each line..." %>
    <%= f.submit %>

<% end %>
